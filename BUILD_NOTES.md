I created a simple static site for the Open Architecture project. Files added:

- index.html (landing page)
- search.html (explore designs & architects)
- design.html (single design view)
- login.html, signup.html (auth forms, mock)
- account.html (user account page)
- upload.html (design upload form)
- assets/css/styles.css (stylized dark theme)
- assets/js/app.js (minimal client interactions)
- assets/js/data.js (mock dataset)
- assets/js/search.js (search UI)
- assets/images/{sample-design.jpg,sample-design-2.jpg,avatar.png} (placeholders)

Notes:
- All interactivity is client-side mock; no backend provided. Forms show mock alerts.
- Images are placeholders (0-byte files) — replace with real images.

Next steps you might want me to take:
- Hook up a backend (API) for auth, upload, and persistent data storage
- Add real design image assets and downloadable plan files
- Implement accounts and persistent user sessions
- Improve accessibility and add i18n

If you'd like, I can: generate sample placeholder images, wire a simple JSON-based mock backend, or create a build script.
